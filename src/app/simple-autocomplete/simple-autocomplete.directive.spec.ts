import { SimpleAutocompleteDirective } from './simple-autocomplete.directive';

describe('SimpleAutocompleteDirective', () => {
  it('should create an instance', () => {
    const directive = new SimpleAutocompleteDirective();
    expect(directive).toBeTruthy();
  });
});
